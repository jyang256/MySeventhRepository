let express = require('express');
let router = express.Router();

// TODO: Part 2, require student routes

module.exports = function (db) {
  // TODO: Part 2, have the router use studentsRouter
  return router;
};
